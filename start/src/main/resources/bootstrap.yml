spring:
  application:
    name: shop-membership
  cloud:
    nacos:
      config:
        server-addr: 47.101.178.174:8849
        file-extension: yaml
  # 最终应使用 -Dspring.profiles.active=<profile> 而非硬编码在配置中
  profiles:
    active: dev